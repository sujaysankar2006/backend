Goal: Get your environment ready and learn the tools you’ll use.

lesson:
	1.	Install Python, VS Code, Git, SQLite, DB Browser for SQLite.
	2.	Create virtualenvs, use pip, basic project layout.
	3.	Intro to Git + GitHub (push/pull branches).

  GITHUB:
     1.to download github 
     2.add/create  new repo
     3.create new folder on window
     4.connect github and floder by git commands
     5.create a file on own folder 
     6. add the file in repo by git add
     7.commit the file as message by git commit -m"new file created "
     8.to push file on repo  to roots by git push orign main/roots
 DATABASE COMMANDS:
          sql is a caseinsensetive so use camelcase,upper and lower case
         which is basis of CRUD -Create,Use,Read,Delete
               1..sqlite3 filename(history.db)
               2..tables for showing tables
               3..schema LOGS showing schemas
               4.SELECT * FROM LOGS;
               5.INSERT INTO LOGS (input_text, sha256_hash) VALUES ('hello', 'abcdef123456');
               6.DELETE FROM LOGS;
               7.DROP TABLE LOGS;
 PYTHON:
     to create and store the inputs data from user and stored in data base using sqlite
     use asyncio for multiple user at same time so, share proper resource
     use hashlib file for prevernt the data from unauthorized acces 
     which encryped with char,value

     
